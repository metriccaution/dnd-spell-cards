language: node_js
script:
  # Build the app
  - npm run build
node_js:
  - "10"
cache:
  directories:
    - "node_modules"
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist/static
  on:
    branch: master
